<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.projectminibook"
    tools:ignore="GoogleAppIndexingWarning">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectMiniBook"
        >
        <activity android:name=".listTodoDay"></activity>
        <activity android:name=".updateUser" />
        <activity android:name=".Dangky" />
        <activity android:name=".DangNhap">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".updateDinary" />
        <activity
            android:name=".UserInfor"
            tools:ignore="Instantiatable" />
        <activity android:name=".PostDnr" />
        <activity
            android:name=".dinary_form"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".AddTodo"
            tools:ignore="Instantiatable" />
        <activity android:name=".updateTodo" />
        <activity
            android:name=".ListTodo"
            tools:ignore="Instantiatable" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <receiver android:name=".AlarmReceiver"></receiver>
    </application>

</manifest>